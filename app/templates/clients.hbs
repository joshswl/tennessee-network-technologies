<div class="section">
  <div class="container">
    <div class="title">
      <h1 class="title-text">Our Clients</h1>
    </div>
    <p>Tennessee Network Technologies is experienced in a
      vast array of services across all industries.
      The following is an abbreviated list of where we have
      provided support in our most commonly served industries
      in the past.</p>
    <h1 class="title-text">Retail</h1>
    <div class="grid">
      {{#each retail as |photo|}}
        <div class="grid__item logo-width">
          <div class="logo__card">
            <img src="{{photo.src}}" alt="" class="logo">
          </div>
        </div>
      {{/each}}
    </div>
    <h1 class="title-text">Financial</h1>
    <div class="grid">
      {{#each financial as |photo|}}
        <div class="grid__item logo-width">
          <div class="logo__card">
            <img src="{{photo.src}}" alt="" class="logo">
          </div>
        </div>
      {{/each}}
    </div>
    <h1 class="title-text">Hospitality</h1>
    <div class="grid">
      {{#each hospitality as |photo|}}
        <div class="grid__item logo-width">
          <div class="logo__card">
            <img src="{{photo.src}}" alt="" class="logo">
          </div>
        </div>
      {{/each}}
    </div>
    <h1 class="title-text">Food Services</h1>
    <div class="grid">
      {{#each foodService as |photo|}}
        <div class="grid__item logo-width">
          <div class="logo__card">
            <img src="{{photo.src}}" alt="" class="logo">
          </div>
        </div>
      {{/each}}
    </div>
    <h1 class="title-text">Health Care</h1>
    <div class="grid">
      {{#each healthCare as |photo|}}
        <div class="grid__item logo-width">
          <div class="logo__card">
            <img src="{{photo.src}}" alt="" class="logo">
          </div>
        </div>
      {{/each}}
    </div>
  </div>
</div>
